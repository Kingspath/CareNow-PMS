{
  "name": "carenow",
  "version": "1.0.0",
  "private": true,
  "description": "A multi-tenant SaaS patient-management system for South African healthcare clinics.",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "dev:backend": "npm run dev --workspace=backend",
    "install:all": "npm install && npm install --workspace=backend && npm install --workspace=frontend"
  },
  "author": "CareNow Dev Team"
}